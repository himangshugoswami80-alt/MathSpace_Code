# Architecture
Browser/mobile → Next.js/Flutter → FastAPI → math/AI services → PostgreSQL.
WebSocket endpoint `/ws` provides realtime communication.
Secrets are supplied through environment variables.
